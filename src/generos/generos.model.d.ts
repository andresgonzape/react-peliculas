export interface generoCreacionDTO {
    nombre: string;
}

//Este es para lectura, el de arriba para creacion. Este de aqui deberia ser cogido de base de datos en principio
export interface generoDTO {
    id: number;
    nombre: string;
}